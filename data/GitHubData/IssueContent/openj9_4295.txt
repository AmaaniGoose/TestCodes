Nearly 100% of VM changes are platform agnostic, so it would be nice to be able to launch a PR build on the next available machine, regardless of architecture.

Maybe `jenkins test sanity any jdk8` ?